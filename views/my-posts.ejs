<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
    <link rel="icon" type="image/png" sizes="32x32" href="/image/abisiniy.png">
  <title>My Job Posts Abisniya</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      max-width: 800px;
      margin: auto;
      background: #f9f9f9;
    }
    h1 {
      color: #007bff;
      margin-bottom: 20px;
    }
    .job {
      background: #fff;
      padding: 15px 20px;
      margin-bottom: 15px;
      border-radius: 8px;
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
      position: relative;
    }
    .job h3 {
      margin: 0 0 10px;
      color: #007bff;
    }
    .job p {
      margin: 5px 0;
      color: #333;
    }
    form.delete-form {
      position: absolute;
      top: 15px;
      right: 20px;
    }
    button.delete-btn {
      background: #d33;
      color: white;
      border: none;
      padding: 6px 12px;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;}
    button.delete-btn:hover {
      background-color: #a00;
    }
    a.back-link {
      display: inline-block;
      margin-top: 20px;
      color: #007bff;
      text-decoration: none;
      font-weight: bold;}
    a.back-link:hover {
      text-decoration: underline;}
  </style>
</head>
<body>
  <h1>My Job Posts - <%= userName %></h1>
  <% if (jobs.length === 0) { %>
    <p>You have not posted any jobs yet.</p>
  <% } else { %>
    <% jobs.forEach(job => { %>
      <div class="job">
        <h3><%= job.title %></h3>
        <p><strong>Company:</strong> <%= job.company %></p>
        <p><strong>Location:</strong> <%= job.location %></p>
        <p><%= job.description %></p>
        <p><strong>Contact Info:</strong>
          <% if(job.telegram) { %>
            Telegram: <a href="<%= job.telegram.startsWith('http') ? job.telegram : 'https://t.me/' + job.telegram %>" target="_blank"><%= job.telegram %></a><br/>
          <% } %>
          <% if(job.whatsapp) { %>
            WhatsApp: <a href="<%= job.whatsapp %>" target="_blank"><%= job.whatsapp %></a><br/>
          <% } %>
          <% if(job.imo) { %>
            IMO: <a href="<%= job.imo %>" target="_blank"><%= job.imo %></a><br/>
          <% } %>
          <% if(job.other_chat) { %>
            Other: <a href="<%= job.other_chat %>" target="_blank"><%= job.other_chat %></a><br/>
          <% } %>
        </p>
        <form class="delete-form" method="POST" action="/delete-post/<%= job.id %>" onsubmit="return confirm('Are you sure you want to delete this post?');">
          <button type="submit" class="delete-btn">Delete</button>
        </form>
      </div>
    <% }) %>
  <% } %>
  <a href="/dashboard" class="back-link">&larr; Back to Dashboard</a>
</body>
</html>
