<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Our Services </title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link rel="icon" type="image/png" sizes="32x32" href="/image/abisiniy.png">
  <style>
  body {
         font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin:0; padding:0; color:#333; line-height:1.6;
      background:#f0f4f8;
            color: #333;
    }
    a { text-decoration:none; }
    .navbar {
      display:flex; justify-content:space-between; align-items:center;
            background: #2c3e50;
    }
    .logo img { height:40px; cursor:pointer;color: #f0f4f8; font-size: 34px;}
    .nav-links { display:flex; gap:20px; }
    .nav-links li a { color:#fff; font-weight:500; padding:5px 10px; transition:all 0.3s ease; }
    .nav-links li a:hover, .nav-links li a.active { background:#00b4d8; border-radius:5px; }
    .menu-toggle { display:none; flex-direction:column; cursor:pointer; gap:5px; }
    .menu-toggle span { width:25px; height:3px; background:#fff; border-radius:2px; }
    header {
      padding:80px 20px; text-align:center;
      background: linear-gradient(120deg, #caf0f8, #34495e);
      overflow:hidden;
    }
    header h1 {
      font-size:3rem; color:#0077b6; margin-bottom:20px;
      transform: translateY(-50px); opacity:0; transition: all 1s ease;
    }
    header p {
      font-size:1.2rem; max-width:700px; margin:auto; color:#03045e;
      transform: translateY(-50px); opacity:0; transition: all 1s ease 0.3s;
    }
    .services-container {
      display:flex; flex-wrap:wrap; gap:30px;
      justify-content:center; max-width:1200px; margin:50px auto;
    }
    .service-card {
      background:linear-gradient(135deg, #e0f7fa, #b2f7ef);
      width:260px; padding:25px; border-radius:20px; text-align:center;
      box-shadow:0 10px 25px rgba(0,0,0,0.1);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      cursor:pointer; opacity:0; transform: translateY(50px);
    }
    .service-card i {
      font-size:3rem; color:#0077b6; margin-bottom:15px; transition: transform 0.3s ease, color 0.3s ease;
    }
    .service-card:hover {
      transform: translateY(-15px);
      box-shadow:0 20px 40px rgba(0,0,0,0.25);
    }
    .service-card:hover i { transform: scale(1.2); color:#00b4d8; }
    .service-card h3 { margin-bottom:10px; color:#0077b6; }
    .service-card p { font-size:0.95rem; color:#333; }
    footer { background:#0077b6; color:#fff; text-align:center; padding:20px; }
    .feedback-btn {
      position:fixed;
      bottom:20px;
      right:20px;
      background:#25D366; 
      color: #34495e;
      padding:15px 20px;
      border-radius:50px;
      font-weight:bold;
      box-shadow:0 5px 15px rgba(0,0,0,0.3);
      transition: transform 0.3s ease, background 0.3s ease;
      z-index:1000;
    }
    .feedback-btn:hover {
      transform: scale(1.1);
      background:#00bfa5;
    }
    @media(max-width:768px){
      .nav-links { position:absolute; right:0; top:60px; background:#0077b6; flex-direction:column; width:100%; display:none; }
      .nav-links li { text-align:center; margin:15px 0; }
      .menu-toggle { display:flex; }
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="logo" onclick="window.location.href='/dashboard'">
      <img src="/image/abisiniy.png" alt="Abisinya Logo">
    </div>
    <ul class="nav-links">
      <li><a href="/login">Home</a></li>
      <li><a href="/about">About</a></li>
      <li><a href="/services" class="active">Services</a></li>
   
    </ul>
    <div class="menu-toggle" onclick="toggleMenu()">
      <span></span><span></span><span></span>
    </div>
  </nav>
  <header>
    <h1 id="header-title">Our Services</h1>
    <p id="header-desc">We offer a wide range of services to help job seekers and employers connect efficiently and successfully.</p>
  </header>
  <main>
    <div class="services-container">
      <div class="service-card">
        <i class="fas fa-briefcase"></i>
        <h3>Job Posting</h3>
        <p>Post job openings easily with verified listings so your company attracts the right talent quickly.</p>
      </div>
      <div class="service-card">
        <i class="fas fa-search"></i>
        <h3>Find Candidates</h3>
        <p>Search and filter profiles to find qualified candidates efficiently with our smart tools.</p>
      </div>
      <div class="service-card">
        <i class="fas fa-robot"></i>
        <h3>AI Recommendations</h3>
        <p>Our AI-driven system suggests the best matches between job seekers and employers.</p>
      </div>
      <div class="service-card">
        <i class="fas fa-headset"></i>
        <h3>Support & Assistance</h3>
        <p>We provide responsive customer support to assist both employers and job seekers anytime.</p>
      </div>
    </div>
  </main>
  <a href="https://t.me/YourTelegramBot" target="_blank" class="feedback-btn">
    Give Feedback
  </a>
  <footer>
    <p>&copy; 2025 Abisniya Jobs. All rights reserved.</p>
  </footer>
  <script>
    function toggleMenu() {
      const navLinks = document.querySelector('.nav-links');
      navLinks.style.display = navLinks.style.display === 'flex' ? 'none' : 'flex';
    }
    window.addEventListener('load', () => {
      document.getElementById('header-title').style.transform = 'translateY(0)';
      document.getElementById('header-title').style.opacity = '1';
      document.getElementById('header-desc').style.transform = 'translateY(0)';
      document.getElementById('header-desc').style.opacity = '1';
    });
    const serviceCards = document.querySelectorAll('.service-card');
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if(entry.isIntersecting){
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, { threshold: 0.2 });
    serviceCards.forEach(card => observer.observe(card));
  </script>
</body>
</html>
